Instalar pacotes: `uv add --dev pyinstaller auto-py-to-exe`

Codigo para criar executavel via interface pyinstller: `uv run auto-py-to-exe`

O arquivo **Executavel.json,** contem o codigo para o auto-py-to-exe ler e executar automaticamente.
